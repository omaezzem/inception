#These settings are for the MariaDB server
[mysqld]

#Your WordPress configuration should therefore use something like: DB_HOST=mariadb:3306
port = 3306
#MariaDB should listen on all network interfaces.
bind-address = 0.0.0.0